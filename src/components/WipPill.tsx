export default function WipPill() {
  return (
    <span className="inline-flex items-center rounded-full border border-[var(--warn)] bg-[var(--warn-soft)] px-2 py-[3px] font-mono text-[9px] tracking-[.12em] text-[var(--warn-text)]">
      WIP
    </span>
  );
}
